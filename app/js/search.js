(function (global) {
  "use strict";
  // Réservé à une Story future : aucune recherche n'est annoncée ni simulée ici.
  global.AtelierCatalog = global.AtelierCatalog || {};
  global.AtelierCatalog.search = { available: false };
}(window));
